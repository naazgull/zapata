/**
 * @file socket.h
 * @brief TCP/UDP/Unix socket streams.
 *
 * This is the aggregate header for socket-based I/O streams.
 * Provides socket client and server streams supporting TCP, UDP,
 * Unix domain sockets, and optional SSL/TLS encryption.
 *
 * @see zpt::socketstream
 * @see zpt::serversocketstream
 */

#pragma once

#include <zapata/net/socket/socket_stream.h>
